version: '3'

services:

    jekyll:
        image: jekyll/jekyll:pages
        command: jekyll serve --watch --incremental
        ports:
            - 4000:4000
        environment:
            JEKYLL_ENV: development
            FORCE_POLLING: "true"
        volumes:
            - .:/srv/jekyll